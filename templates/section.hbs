{{#if image}}
{{#with image}}
<img src="{{imgSrc}}" id="{{id}}">
{{/with}}
{{/if}}
<section>
    <h1>{{name}}</h1>
    {{#each paragraphs}}
    <ul>
        <li>{{this}}</li>
    </ul>
    {{/each}}
</section>